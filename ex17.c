#include <stdio.h>

int main()
{
    int anni;
    printf("inserisci un numero");
    scanf(" %d", &a )
    if (( a / 400 ) && ( a / 100 !=0 ))
    {
        printf("é bisestile");
    }
    else if ( a / 100)
    {
        printf(" non é bisestile");
    }
}